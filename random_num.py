from random inport randint

a = randint(10, 99)*9275
print(a)


# or you can do this way too

print(randint(0, 9), randint(0, 9), randint(0, 9), randint(0, 9), randint(0, 9), randint(0, 9), sep="") #this will automatically generate the new 
# 6 digit random number, using nothing as a seperator(tab, comma, etc can be used as well). This random number can be used in many ways.
